import React from 'react';
import {SafeAreaView, View} from 'react-native';
import {MainScreen} from './src/screens';
const App = () => {
  return (
    <View style={{flex: 1}}>
      <MainScreen />
    </View>
  );
};

export default App;
